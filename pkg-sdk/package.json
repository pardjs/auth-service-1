{
  "name": "@pardjs/auth-service-sdk",
  "version": "0.0.4",
  "description": "The Pardjs Auth Service SDK for common use.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json 'src/*.ts' --fix",
    "test": "jest",
    "prepublish": "yarn run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/jest": "^24.0.23",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
